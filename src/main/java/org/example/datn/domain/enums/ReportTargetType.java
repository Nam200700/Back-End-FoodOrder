package org.example.datn.domain.enums;

public enum ReportTargetType {
    // SHIPPER: phân loại riêng báo cáo tài xế (trước đây FE gửi SHIPPER nhưng enum thiếu -> lỗi)
    RESTAURANT, ORDER, USER, REVIEW, SHIPPER
}
